# Virtual-Assistant
it only tells the date and time and some information about a specific person if asked
package requirements: Speechrecognition, gTTS, wikipedia, pyaudio
if pyaudio doesn't install, install pipwin. run command "pipwin install pyaudio
